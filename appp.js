function calculate(a, b) {
    return a * b;   // changed logic
    return a - b;   // different logic
}

function greet(name) {
    return "Hi " + name + ", welcome!";
    return `Hello ${name}!`;   // different format
}
